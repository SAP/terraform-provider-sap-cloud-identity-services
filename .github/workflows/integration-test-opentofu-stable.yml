name: Integration Test - OpenTofu - Stable Provider

on:
  workflow_dispatch:
  schedule:
    - cron: '0 14 * * 1,3' # Every Monday and Wednesday at 7:30 PM IST (2:00 PM UTC)

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  issues: write
  contents: read

jobs:

  canary-stable:
    name: Canary Stable
    uses: ./.github/workflows/ot-integration-canary-stable.yml
    secrets: inherit

  live-stable:
    name: Live Stable
    uses: ./.github/workflows/ot-integration-live-stable.yml
    secrets: inherit

  failure-handler:
    name: Handle Failures
    needs: [canary-stable, live-stable]
    if: always()
    permissions:
      contents: read
      issues: write
    uses: ./.github/workflows/integration-test-issue-reporter.yml
    with:
      workflow_name: "OpenTofu Integration Tests - Stable Provider"
      job_results: |
        canary-stable: ${{ needs.canary-stable.result }}
        live-stable: ${{ needs.live-stable.result }}
      labels: 'integration-test,opentofu,failure'